<p id=”notice”><%= notice %></p>
<h1>Find Me Food!</h1>

<p>Hello,
<% if current_user.first_name == "" %>
    <%= current_user.email + "."%>
<% else %>
    <%= current_user.first_name + " " + current_user.last_name + "." %>
<% end %>|
<%= link_to "Log Out", logout_path %></p>

<% if current_user.num_ratings == 0%>
    <p>It looks like you have no ratings! Please rate restaurants to have them displayed for easy selection.</p>
<% else %>
    <h2>Previous Restaurants</h2>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>City</th>
                <th>Genre</th>
            </tr>
        </thead>
        <tbody>
            <% current_user.rating.each do |rating| %>
                restaurant = Restaurant.find(rating.restaurant_id)
                <tr>
                    <td>restaurant.name</td>
                    <td>restaurant.city</td>
                    <td>restaurant.genre</td>
                </tr>
            <% end %>
        </tbody>
    </table>
<% end %>